# Eat-n-Split2 (Expense Tracker)

## 🚀 Overview
A small web application made in React to track expenses between friends. This was one of my first web applications built with React and was used to practice React fundamentals including managing state, React hooks & building reusable components. 

## 🛠 Tech Stack
* **Language:** React, CSS

## 📦 Installation
1. "Clone the repository and download to local machine.
2. Run "npm install" in the terminal.
3. Run "npm run dev" in the terminal."


## 🐛Existing Bugs:
- Unable to remove leading zeros from input fields. However, this does not affect any of the mathematical calculations.
- Some values do not have a trailing zero and are left with a single decimal figure. These values do not look like typical monetary values however, this does not affect any of the mathematical calculations.
